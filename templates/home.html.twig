{% extends 'base.html.twig' %}
{% block title %}Home page{% endblock %}
{% block body %}
    <div class="title m-b-md">
        Welcome
    </div>

    <div>
        {% if is_granted('ROLE_USER') %}
            <p>
                <a href="{{ path('app_user_edit' , {'id' :  app.user.id}) }}">Edit Profile</a>
            </p>
        {% endif %}
    </div>
{% endblock %}